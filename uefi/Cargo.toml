[package]
name = "pkbl-uefi"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
uefi = { version = "0.35", features = ["alloc", "panic_handler"] }
proka-bootloader = { path = "../library" }
